# OpenLoop configuration.
# Reference only: OpenLoop does not parse .env files. These values must be in
# the OpenCode server process environment when the plugin loads. For Desktop,
# set persistent user variables before launching the app or use defaults.

# CODER agent configuration -------------------------------------------------
# Model in the form "<providerID>/<modelID>" e.g. "ollama-cloud/glm-4.6".
# Leave blank to use the server/agent default model.
OPENLOOP_CODER_MODEL=
# Agent name to use for the coder (must be a "primary" agent in OpenCode).
OPENLOOP_CODER_AGENT=build

# REVIEWER agent configuration ---------------------------------------------
# Independent model/agent from the coder. May be the same or different.
OPENLOOP_REVIEWER_MODEL=
OPENLOOP_REVIEWER_AGENT=build

# Loop configuration -------------------------------------------------------
# Maximum number of coder->reviewer review rounds before OpenLoop stops.
OPENLOOP_MAX_ROUNDS=6
# When true, arbitrary shell, file-editing, delegation, and loop-control tools
# are denied at the reviewer session boundary.
OPENLOOP_REVIEWER_READONLY=true
# Allow the active reviewer to run approved package.json scripts through the
# constrained openloop_verify tool. This does not grant arbitrary shell access.
OPENLOOP_REVIEWER_VERIFICATION=true
# Comma-separated package.json script names the reviewer may select. Names may
# contain letters, numbers, colon, underscore, and hyphen; commands are not accepted.
OPENLOOP_REVIEWER_VERIFY_SCRIPTS=test,typecheck,lint,build,check
# Per-script verification timeout (ms).
OPENLOOP_VERIFICATION_TIMEOUT_MS=600000
# Per-turn timeout (ms) before a busy session is aborted.
OPENLOOP_TURN_TIMEOUT_MS=1800000
# Status poll interval (ms) used as a fallback when no session event arrives.
OPENLOOP_POLL_INTERVAL_MS=2000
